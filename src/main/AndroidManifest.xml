<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="am.fiap.com.br.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LojistaActivity" />
        <activity android:name=".TelaNavegacao" />
        <activity android:name=".ListaPromocao">

        </activity>
        <activity android:name=".Cadastro_promocoes" />
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".LoginLojista" />
        <activity android:name=".LoginDoador" />
        <activity android:name=".ConfirmaDoacao" />
        <activity android:name=".Confirmar_Troca_Pontos">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>